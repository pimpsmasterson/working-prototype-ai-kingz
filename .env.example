```dotenv
### Environment variables for provisioning
# Minimum disk size (GB) required for provisioning and model storage. Adjust for your model set.
MIN_DISK_GB=200

# Set to "true" to attempt automatic installation of NVIDIA drivers and nvidia-container-toolkit
# Warning: may require reboot and may fail on some hosted environments (Secure Boot, unsupported images)
INSTALL_NVIDIA_DRIVERS=false

# Tokens used by provisioning: set these before running the script
# Example: export CIVITAI_TOKEN="your_token"
CIVITAI_TOKEN=
HUGGINGFACE_HUB_TOKEN=
# AI KINGS - Environment Variables Template
# Copy this file to .env and fill in your actual keys.
# NEVER commit your actual .env file to version control.

# --- VAST.AI CONFIGURATION ---
# Your Vast.ai API Key from https://cloud.vast.ai/account/
VASTAI_API_KEY=your_vastai_api_key_here

# --- SERVER AUTHENTICATION ---
# Secure key for accessing admin endpoints
ADMIN_API_KEY=secure_admin_key_change_me
# Salt for HMAC-based audit logging (keep this consistent)
AUDIT_SALT=ai_kings_audit_salt_2026_random_entropy

# --- MODEL DOWNLOAD TOKENS ---
# Required for gated models on Hugging Face (e.g. Flux, Wan 2.2)
HUGGINGFACE_HUB_TOKEN=your_hf_token_here
# Required for Civitai downloads
CIVITAI_TOKEN=your_civitai_token_here
# Dropbox token for automating shared link creation (scopes: files.content.read, files.metadata.read, sharing.write)
DROPBOX_TOKEN=your_dropbox_token_here
# Folder path in Dropbox to publish links from (e.g. /Models or /MyModels)
DROPBOX_FOLDER=/MyModels

# --- PROVISIONING CONFIGURATION ---
# Base URL for modular scripts (e.g. GitHub raw URL)
SCRIPTS_BASE_URL=https://gist.githubusercontent.com/pimpsmasterson/c3f61f20067d498b6699d1bdbddea395/raw?v=3.0
# The main entry point script URL (points to the modular setup.sh)
COMFYUI_PROVISION_SCRIPT=https://gist.githubusercontent.com/pimpsmasterson/c3f61f20067d498b6699d1bdbddea395/raw?v=3.0
# Optional: GitHub token with gist scope for pushing provision script (scripts/push-provision-to-gist.js)
# GITHUB_TOKEN=your_github_personal_access_token_here

# --- SERVER SETTINGS ---
PORT=3000
WARM_POOL_SAFE_MODE=0
WARM_POOL_IDLE_MINUTES=15
WARM_POOL_DISK_GB=600
COMFYUI_TUNNEL_URL=http://localhost:8188

# --- REVERSE SSH TUNNEL (optional) ---
# Use either `REVERSE_SSH_DEST` (user@host[:port]) or `REVERSE_SSH_USER` + `REVERSE_SSH_HOST`.
# REVERSE_SSH_DEST=user@example.com[:22]   # full destination (user@host or user@host:port)
# REVERSE_SSH_USER=user                    # split form: user for reverse SSH
# REVERSE_SSH_HOST=example.com             # split form: host for reverse SSH
# REVERSE_SSH_KEY=/root/.ssh/id_rsa        # optional private key file for SSH auth
# REVERSE_TUNNEL_PORT_START=26700          # fallback remote port range start (inclusive)
# REVERSE_TUNNEL_PORT_END=26799            # fallback remote port range end (inclusive)

```
### Environment variables for provisioning
# Minimum disk size (GB) required for provisioning and model storage. Adjust for your model set.
MIN_DISK_GB=200

# Set to "true" to attempt automatic installation of NVIDIA drivers and nvidia-container-toolkit
# Warning: may require reboot and may fail on some hosted environments (Secure Boot, unsupported images)
INSTALL_NVIDIA_DRIVERS=false

# Tokens used by provisioning: set these before running the script
# Example: export CIVITAI_TOKEN="your_token"
CIVITAI_TOKEN=
HUGGINGFACE_HUB_TOKEN=
# AI KINGS - Environment Variables Template
# Copy this file to .env and fill in your actual keys.
# NEVER commit your actual .env file to version control.

# --- VAST.AI CONFIGURATION ---
# Your Vast.ai API Key from https://cloud.vast.ai/account/
VASTAI_API_KEY=your_vastai_api_key_here

# --- SERVER AUTHENTICATION ---
# Secure key for accessing admin endpoints
ADMIN_API_KEY=secure_admin_key_change_me
# Salt for HMAC-based audit logging (keep this consistent)
AUDIT_SALT=ai_kings_audit_salt_2026_random_entropy

# --- MODEL DOWNLOAD TOKENS ---
# Required for gated models on Hugging Face (e.g. Flux, Wan 2.2)
HUGGINGFACE_HUB_TOKEN=your_hf_token_here
# Required for Civitai downloads
CIVITAI_TOKEN=your_civitai_token_here
# Dropbox token for automating shared link creation (scopes: files.content.read, files.metadata.read, sharing.write)
DROPBOX_TOKEN=your_dropbox_token_here
# Folder path in Dropbox to publish links from (e.g. /Models or /MyModels)
DROPBOX_FOLDER=/MyModels

# --- PROVISIONING CONFIGURATION ---
# Base URL for modular scripts (e.g. GitHub raw URL)
SCRIPTS_BASE_URL=https://gist.githubusercontent.com/pimpsmasterson/c3f61f20067d498b6699d1bdbddea395/raw?v=3.0
# The main entry point script URL (points to the modular setup.sh)
COMFYUI_PROVISION_SCRIPT=https://gist.githubusercontent.com/pimpsmasterson/c3f61f20067d498b6699d1bdbddea395/raw?v=3.0

# --- SERVER SETTINGS ---
PORT=3000
WARM_POOL_SAFE_MODE=0
WARM_POOL_IDLE_MINUTES=15
COMFYUI_TUNNEL_URL=http://localhost:8188
